export const metadata = {
    title:"Register",
}
export default function layoutRegister({children}){

    return(
        <section>
            aqui va contenido del layout del register
            {children}
        </section>
    )
}